Ceci est une application de RAG (Retrieval-Augmented Generation) avec un chatbot. 

Pour l'utiliser, il faut installer les dépendances et lancer l'application.

Installation des dépendances :
```bash
pip install -r requirements.txt
```

Lancement de l'application :
```bash
streamlit run streamlit/app.py
```